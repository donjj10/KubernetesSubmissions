apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-image-pvc

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: todo-image-manual